<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Base XML theme used only until the Compose UI takes over.
      The real design system lives in AuroraScanTheme (Compose / Material 3).
    -->
    <style name="Theme.AuroraScan" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>
</resources>
